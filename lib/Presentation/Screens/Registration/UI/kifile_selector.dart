
import 'package:flutter/material.dart';
class KifileSelecter extends StatelessWidget {
  const KifileSelecter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Column(
      children: const [
        Text('ለለመዝገብ የፈለጉት አባል'),


      ],
    );
  }
}
